import { FilterdistributorsPipe } from './filterdistributors.pipe';

describe('FilterdistributorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterdistributorsPipe();
    expect(pipe).toBeTruthy();
  });
});
